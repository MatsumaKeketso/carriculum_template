import React from 'react'
import Typography from '../Typography'

const SegmentItem = ({label}) => {
  return (
    <div><Typography variant="s2">{ label }</Typography></div>
  )
}

export default SegmentItem